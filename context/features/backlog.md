# Backlog — ideas and unfinished business not scheduled in any wave

Purpose: a single place for things Michal wants **eventually**, so they are not lost inside a finished feature file.
An item here is **not a decision and not a requirement**: before building one, design it in a feature folder and get
Michal's approval. When an item is scheduled into a wave, move it there and delete it here. Keep entries short: what,
why, where it came from, what is already known.

BL-12 and BL-13 are temporarily retained as stable cross-reference IDs because existing designs, reviews and code
comments cite them. Their scheduling status is authoritative in their rows: BL-13 / wave 5 is next; BL-12 / wave 4
follows it. Do not renumber either wave.

| id | Item | Why / origin | Known so far |
|---|---|---|---|
| BL-01 | **Requests to the manager ("REQUEST")** — a separate concept from wave-3 tasks (and different from the old round-1 "požiadavky"): placeable on **any lead**, including one that was reassigned away and sits in the SR's História; it sends the manager a message to do something for you — e.g. reopen a lead, or reopen and reassign it. | Michal, 2026-09-19 (wave 3 D13): reopen is not a task — a task = the manager delivers something the SR then uses. | Wave 3 removed `DealRequest` (incl. the REOPEN kind and `ClosedPolicy "reopenRequestOnly"`); reopening is manager-only (`reopenDealAs`). Until BL-01 exists the SR tells the manager outside the app and the manager reopens. Open: which request types exist, who may ask about a deal they no longer own, where the manager sees them (likely next to "Pre mňa"). |
| BL-02 | **Fixed client appointment** ("pošlite cenu a zavolajte v pondelok o 9:00") next to the next step | Wave 3 discussion, decision D11 in `01-salesrep/wave-3-task-proposal-final.md` | The app is a next-step machine: one step per deal. A relative follow-up after a send exists. Candidate: a separate "dohodnutý termín" (date, optional time, note) that also drives Na dnes and pre-fills the send dialog's follow-up. Only if it turns out to happen in practice. |
| BL-03 | **Price per návrh / offer model** | Wave 3 discussion, finding F2 in `01-salesrep/wave-3-task-proposal-final.md` | `OFFER_SENT` already records what went together (contents, price snapshot, design snapshots). Missing: one `Lead.price` per deal (no price per návrh); "Cena & ponuky" does not show which price went with which návrh. A core offer-model change; design before anyone touches `Lead.price`. Michal's direction (2026-09-19): record what the client **wants** by ticking contents ("Chcú: cena, návrh…"), and what **I sent** by ticking contents — the next wave or so. |
| BL-04 | **Developer projects** — a developer role with its own project and back-and-forth communication ("change this, this, this") | Decision D10 in `01-salesrep/wave-3-task-proposal-final.md`; round 2 §6 item 5 | Not built on wave 3 tasks. Should reference the deal/design and have its own access and work queue; must not inherit SR deal visibility. |
| BL-05 | **Pipeline sort choice** ("naposledy upravené" vs "podľa ďalšieho kroku") | `01-salesrep/round2-deal-workspace.md` §2d "Notes for later" | With ~200 deals, the deal you just touched is hard to find. |
| BL-06 | **Team sanity warnings** (e.g. a telesales caller whose team has no rep to receive deals) | Wave 2/3 discussions | Deferred from wave 3. |
| BL-07 | **Per-content milestones for a combined task** (separate time-to-price and time-to-návrh; partial delivery of a price+návrh task) | ChatGPT review W3-R16, 2026-09-19 | Wave 3 has one completion time per task; accepted limit. |
| BL-08 | **F-01 cenník in the app** (pickable price items) | `01-salesrep/round2-deal-workspace.md` §2 / §6 item 6 | Parked; later round-2 notes say the structured `priceItems` were dropped. Re-decide before the pricing wave. |
| BL-09 | **Order / WON process** — a WON step with final prices and details; what was ordered from several products (web, SEO, marketing, social media) | `01-salesrep/round2-deal-workspace.md` §7 non-goals; Michal on "Chcú objednať" (wave 3 D15): *"order is pre-made, because we also have different things, like SEO, marketing, social media… we will need to remake this in the future"* | Wave 3 removed the ORDER step and ORDER request; "Chcú objednať" is a recorded reply that offers a handover. After pricing. |
| BL-10 | **Statistics rebuild** (`/dashboard/stats`), incl. manager turnaround from task timestamps | round 2 §7 non-goals; wave 3 task timestamps | Not scheduled. |
| BL-11 | **"Transferred" marker and filter** in the manager's pipeline — e.g. a blue dot on deals that were moved to him, and a filter "presunuté" | Michal, 2026-09-19 (wave 3 review W3-R02, deactivation) | Data exists from wave 3 on (`DealOwnership`). |
| BL-12 | **One task for price + návrh** — **scheduled after wave 5; wave 4 Part A is blocked**, see `01-salesrep/wave-4-proposal.md` §2 (both manager-work parts selectable in any order, partial delivery with ✓ per part) | Michal, 2026-09-19 | Wave 4 consumes wave 5's shipped remaining-to-send projection; it must not own client intent or recreate the combined send checklist. Re-review wave 4 after wave 5, then delete this row when the wave-4 design is final. |
| BL-13 | **Wave 5 — NEXT: remake the price / návrh / email selection**; record what the client **asked for** vs. what we **sent anyway**, derive the combined remaining-to-send checklist, allow partial sending, and preserve correction/revival | Michal, 2026-09-19; implementation-order decision: stable order is wave-3 closeout → wave 5 → wave 4 | Active discussion draft: `context/features/01-salesrep/wave-5-proposal.md`. It must work with current wave-3 single-content tasks and becomes the prerequisite contract for wave 4. Asked and sent may differ on purpose; exact-price visibility remains derived from "Klient dostal". |
